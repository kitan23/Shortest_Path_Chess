#include <utility>
#include <vector>
#include <iostream>
#include "tree.h"

int main()
{
   std::cout << "Enter current position of knight: ";
   int x = 0;
   int y = 0;
   std::pair<int, int> current_coord = std::make_pair(x, y);
   // initialize a tree with the initial coordinates
   return 0;
}
